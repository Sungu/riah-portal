<li id="comment_<%=comment.id %>">
  <%= comment.body %>,
  <%= time_ago_in_words(comment.created_at) %> ago
  <%= link_to '', [comment.post, comment], method: :delete, remote: true, data: { confirm: "Are you sure?" } ,class: 'glyphicon glyphicon-remove' %>
</li>